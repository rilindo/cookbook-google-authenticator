language: ruby
rvm:
  - 2.0.0
before_script:
  - bundle exec berks install
  - bundle exec foodcritic install
  - bundle exec rspec install
  - bundle exec rubocop install
script:
  - bundle exec foodcritic -f any .
  - bundle exec rspec --color --format progress
  - bundle exec rubocop